import statistics

numbers = [1, 2, 3, 4, 5, 6]

# mean => Average
mean = statistics.mean(numbers)
print(f"Mean: {mean}")

# grade_std_dev
std_deviation = statistics.stdev(numbers)
print(f"standard dev: {std_deviation}")
